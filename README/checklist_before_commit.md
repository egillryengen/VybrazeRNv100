Kort sjekkliste før commit (lim inn i PR‑mal)
[ ] yarn install --frozen-lockfile (eller yarn install hvis du genererer lockfile) kjørt lokalt

[ ] npx tsc --noEmit passert lokalt

[ ] npx jest --runInBand passert lokalt

[ ] yarn.lock er committet hvis den ble endret

[ ] package-lock.json er ikke committet